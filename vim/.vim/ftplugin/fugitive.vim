" NOTE: Using <LocalLeader> here is currently not working, as it will get
" shadowed by the global mapping used for text folding. Thus I have to use
" <Leader> as a workaround for now.
"nmap <buffer> <LocalLeader><Space> =
nmap <buffer> <Leader><Space> =
